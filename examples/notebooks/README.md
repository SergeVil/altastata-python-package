# Notebooks

- **altastata.ipynb** – Demo notebook for the AltaStata API and usage patterns.

Run from repo root: `pip install -e .` then open in Jupyter from this directory or from the Jupyter container (mount this folder if needed).
